[project]
name = "echo-frb"
version = "0.0.1"
description = "Project ECHO-FRB — CHIME/FRB Catalog 2 data-preparation pipeline (WP0)"
requires-python = ">=3.10"
dependencies = [
    "h5py",
    "numpy",
    "scipy",
    "astropy",
    "xarray",
    "pandas",
    "pyarrow",
    "matplotlib",
    "pyyaml",
    "zarr",
]

[project.optional-dependencies]
dev = ["pytest"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
