# WP1 Candidate Reproduction Report

Independent evaluation of the two named candidates and the published selection chain (proposal §12.2),
structured per the **Appendix B Minimum Candidate Audit Record**. Verdicts use a **reproduction**
vocabulary — REPRODUCED-ROBUST / NOT-INDEPENDENTLY-REPRODUCED — which is **not** the §7 discovery grade
(those require the WP4 catalog-global false-alarm probability, not computed here).

---

## Candidate A — FRB 20190131D  →  **REPRODUCED-ROBUST**

| Record group | Contents |
|---|---|
| **Identity** | Catalog burst FRB 20190131D; in the 340 multi-peak set (`chimefrbcat2_first_duplicates.npy`). One of the two named holdout candidates. |
| **Data integrity** | Tier A `<TNS>_stokesi_dynamic_spectrum.h5` SHA-256 recorded in `raw_archive_manifest.parquet`; **byte-identical to the CANFAR release** (verified W1.1). Tier B generated by the frozen pipeline (`--process-candidates`), provenance-stamped (`content_sha256`). |
| **Preprocessing** | Literal: authors' `process_data_ts` (f_down=32, rfi_factor=3). Clean-room: our Tier B (inverse-variance light curve), `config_hash` recorded. |
| **Search** | Literal: Δt=**8.82 ms**, matched pair **[76,85]**, has_drift=False. Clean-room: Δt=**8.847 ms** (within 1 bin), same pair **[76,85]**, mag_ratio≈**2.51**, K-S D=0.033 < D_upp=0.083 (achromatic). |
| **Significance** | LOCAL detection only. Catalog-global FAP **not computed** (WP4). ACF spike >3σ in both tracks. |
| **Robustness** | **Survival fraction 1.00** — candidate in **all 20** swept configs (17 literal axes + 3 clean-room thresholds). W1.4 factorial: **AGREE** (detected in all four LC×ACF cells). |
| **Model comparison** | N-A in WP1 (generative inference is WP5). |
| **External checks** | Baseband/polarization availability deferred to WP5; localization/foreground work not attempted (approximate Catalog 2 localization). |
| **Decision** | **REPRODUCED-ROBUST.** Recovered by the authors' code and by a fully independent, blind re-implementation on independent preprocessing, at the same delay and peak pair, and stable across every preprocessing/detection choice tested. Not a discovery claim; warrants continued attention in later WPs. |

---

## Candidate B — FRB 20211115A  →  **NOT-INDEPENDENTLY-REPRODUCED (FRAGILE)**

| Record group | Contents |
|---|---|
| **Identity** | Catalog burst FRB 20211115A; in the 340 multi-peak set. Second named holdout candidate. |
| **Data integrity** | Same as A — Tier A SHA-256 in the manifest, CANFAR-identical; Tier B frozen-pipeline generated and hashed. |
| **Preprocessing** | Literal: authors' `process_data_ts`. Clean-room: our Tier B. Config hashes recorded both tracks. |
| **Search** | Literal: Δt=**6.86 ms**, matched pair **[78,85]**, has_drift=False. Clean-room: **no significant ACF spike** (n_spikes=0) → no matched pair, no delay, no copy test reached. |
| **Significance** | LOCAL only; catalog-global FAP not computed (WP4). Literal spike >3σ; clean-room sub-threshold at **all** kσ down to 2.0. |
| **Robustness** | **Survival fraction 0.882** — drops out under **SG_100** and **spike threshold kσ=4**; **never recovered by the clean-room** at any threshold. W1.4 factorial: **MIXED** — both the detection algorithm and our preprocessing suppress the spike. |
| **Model comparison** | N-A in WP1. |
| **External checks** | Deferred to WP5; additionally **absent under the authors' own SG_100 smoothing config** — fragile from three independent directions (independent algorithm, our preprocessing, authors' own alternate config). |
| **Decision** | **NOT-INDEPENDENTLY-REPRODUCED / FRAGILE.** Reproducible with the authors' exact code + preprocessing + a permissive spike threshold, but not under an independent implementation and not across smoothing/threshold variants. Should not be treated as a robust candidate on catalog data alone. |

---

## Selection chain — reproduction status

| Stage | Reported | Reproduction |
|---|---|---|
| Processed multi-peak set | 340 | **EXACT** (literal + clean-room). |
| G_3 initial candidates | 11 | **Literal EXACT**; clean-room 1 (divergence at SPIKE stage, attributed). |
| SG_20 / SG_100 initial | 16 / 12 | **Literal EXACT**; clean-room N-A (single smoothing). |
| Morphology reassessment | 11 → 9 | **SUBJECTIVE** — manual, not algorithmically specified; not reproduced (inputs reproduced). |
| Final candidates | 9 → 2 | Depends on the subjective step; clean-room independently yields 1. |

**Selection-chain conclusion:** the *algorithmic* portion of the chain reproduces exactly under the
authors' code; the reduction from 11 to the final 2 rests on a manual morphology reassessment that is
not specified well enough to reproduce, and the initial count itself is contingent on an
under-documented smoothing/threshold choice (see `docs/WP1_W1.5_findings.md`).
